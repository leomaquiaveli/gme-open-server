---
name: Bug report
about: Something isn't working as expected
labels: bug
---

**Describe the bug**
A clear description of what happened and what you expected.

**Environment**
- OS:
- Go version (`go version`):
- FFmpeg version (`ffmpeg -version | head -1`):
- GPU / encoder (`GET /health` response):
- Storage type (`local` or `gcs`):

**Request**
```json
// paste the JSON payload you sent
```

**Response or webhook payload**
```json
// paste what you received
```

**Additional context**
Any relevant logs or configuration (redact API keys and credentials).
